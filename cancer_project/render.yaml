services:
  - type: web
    name: cancer-prediction-system
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --no-input
    startCommand: gunicorn cancer_project.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
